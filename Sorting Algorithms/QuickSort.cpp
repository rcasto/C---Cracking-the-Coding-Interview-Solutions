#include <iostream>

int *quicksort(int *array, int left, int right) {
	if (!array) {
		return 0;
	}
	int length = right - left;
	if (length <= 1) {
		return array;
	}
}

/*
	Debugging/Testing
*/
int main(int argc, char *argv[]) {
	return 0;
}